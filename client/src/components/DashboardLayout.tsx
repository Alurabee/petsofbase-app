// DashboardLayout is not used in PetsOfBase
// This is a template component for admin dashboards
// PetsOfBase uses Navigation component instead

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
